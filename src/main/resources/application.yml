spring:
  application:
    name: ai-resume-screener
  servlet:
    multipart:
      max-file-size: 10MB
      max-request-size: 10MB
  ai:
    openai:
      api-key: ${OPENAI_API_KEY:dummy-key-for-local-testing}
server:
  port: 8081
